import { NgxLocalePercentageMaskDirective } from './ngx-locale-percentage-mask.directive';

describe('NgxLocalePercentageMaskDirective', () => {
  it('should create an instance', () => {
    // const directive = new NgxLocalePercentageMaskDirective();
    // expect(directive).toBeTruthy();
  });
});
